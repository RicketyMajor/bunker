# Android Studio writes this file when it creates a project; this one was made by hand, so it
# did not exist and `:app:checkDebugAarMetadata` failed on the first build. Every androidx
# dependency in app/build.gradle.kts needs it.
android.useAndroidX=true
